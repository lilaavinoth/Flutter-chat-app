name: chat_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  firebase_core: ^2.8.0
  flutter:
    sdk: flutter
  go_router: ^6.5.0
  google_sign_in: ^6.0.2
  cloud_firestore: ^4.4.5
  uuid: ^3.0.7
  crypto: ^3.0.2
  gap: ^2.0.1
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
